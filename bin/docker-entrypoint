#!/bin/bash
set -e

if [ -f tmp/pids/server.pid ]; then
  rm tmp/pids/server.pid
fi

if [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ]; then
  echo "Deploying: Running Database Migrations..."
  ./bin/rails db:migrate
fi

exec "$@"